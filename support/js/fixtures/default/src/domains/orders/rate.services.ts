// elda/no-diagonal-reach-gate (SURFACE.5, across-domains): the binding launders through cart's
// barrel and lands on a foreign flow, below this services unit's own rank.
import { withVat } from '#/cart';

export const rate = (n: number) => withVat(n);
