// Shipped and never composed: the print entry does not import this file, so its bytes travel and nothing runs them - the dead-bundle case the entry directive exposes.
export const staleTotals = () => 0;
